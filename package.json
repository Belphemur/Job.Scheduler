{
  "name": "Job.Scheduler",
  "release": {
    "plugins": [
      "@semantic-release/commit-analyzer",
      "@semantic-release/release-notes-generator",
      "@semantic-release/changelog",
      [
        "@semantic-release/exec",
        {
          "prepareCmd": "dotnet pack -v normal -c Release --include-symbols --include-source -o nupkg -p:PackageVersion=${nextRelease.version} -p:IncludeSymbols=true -p:SymbolPackageFormat=snupkg ${process.env.PROJECT_NAME}/${process.env.PROJECT_NAME}.*proj",
          "publishCmd": "dotnet nuget push nupkg/*.nupkg --source ${process.env.NUGET_FEED} --skip-duplicate --api-key ${process.env.NUGET_KEY}"
        }
      ],
      "@semantic-release/git",
      [
        "@semantic-release/github",
        {
          "assets": [
            {
              "path": "nupkg/*.*nupkg"
            }
          ]
        }
      ]
    ]
  },
  "devDependencies": {
    "@semantic-release/changelog": "^5.0.1",
    "@semantic-release/commit-analyzer": "^8.0.1",
    "@semantic-release/exec": "^5.0.0",
    "@semantic-release/git": "^9.0.0",
    "@semantic-release/github": "^7.2.1",
    "@semantic-release/release-notes-generator": "^9.0.2",
    "semantic-release": "^17.4.2"
  }
}
